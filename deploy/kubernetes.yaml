apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worthboard-data
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 2Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: worthboard-backups
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      storage: 5Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: worthboard
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: worthboard
  template:
    metadata:
      labels:
        app: worthboard
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1001
        runAsGroup: 1001
        fsGroup: 1001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worthboard
          image: ghcr.io/your-user/worthboard:latest
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: DATABASE_PATH
              value: /data/worthboard.db
            - name: BACKUP_PATH
              value: /backups
            - name: APP_URL
              value: https://worthboard.example.com
            - name: TZ
              value: Africa/Nairobi
            - name: SESSION_SECRET
              valueFrom:
                secretKeyRef:
                  name: worthboard-secrets
                  key: session-secret
          volumeMounts:
            - name: data
              mountPath: /data
            - name: backups
              mountPath: /backups
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: [ALL]
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 30
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              memory: 1Gi
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: worthboard-data
        - name: backups
          persistentVolumeClaim:
            claimName: worthboard-backups
---
apiVersion: v1
kind: Service
metadata:
  name: worthboard
spec:
  selector:
    app: worthboard
  ports:
    - name: http
      port: 80
      targetPort: http
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: worthboard
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: 26m
spec:
  ingressClassName: nginx
  tls:
    - hosts: [worthboard.example.com]
      secretName: worthboard-tls
  rules:
    - host: worthboard.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: worthboard
                port:
                  name: http
